name: flutter_touch_spin_2
description: >- 
  A re-upload of flutter_touch_spin (https://pub.dev/packages/flutter_touch_spin).
  
  Reason: to keep dependencies up to date with the latest version of Reactive Forms, to use in the reactive_touch_spin widget.
version: 0.0.3
homepage: https://github.com/benjifarquhar/flutter_touch_spin_2

environment:
  sdk: ">=2.12.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.19.0

dev_dependencies:
  flutter_lints: ^1.0.4
  flutter_test:
    sdk: flutter

flutter:
